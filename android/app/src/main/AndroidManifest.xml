<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <!-- No INTERNET, RECORD_AUDIO, READ_CONTACTS or privileged capture permission:
         this P0 build neither communicates nor records. -->
    <application
        android:name=".TriDefenseApplication"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity android:name=".ui.MainActivity" android:exported="true" />
        <activity android:name=".ui.demo.DemoActivity" android:exported="true" android:label="Tri-Defense Demo" android:icon="@drawable/demo_icon" android:theme="@style/Theme.TriDefense.Demo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.demo.ProtectionDemoActivity" android:exported="true" android:label="Tri-Defense Protection Demo" android:theme="@style/Theme.TriDefense.Demo" />
        <service
            android:name=".screening.TriDefenseCallScreeningService"
            android:exported="true"
            android:permission="android.permission.BIND_SCREENING_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>
    </application>
</manifest>
